generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Hero {
  id           Int     @id @default(autoincrement())
  heroTitle    String
  heroSubTitle String?
  heroDescr    String
}
